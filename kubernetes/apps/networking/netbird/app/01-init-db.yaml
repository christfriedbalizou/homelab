apiVersion: batch/v1
kind: Job
metadata:
  name: netbird-init-db
spec:
  template:
    spec:
      containers:
        - name: init-db
          image: ghcr.io/home-operations/postgres-init:18
          envFrom:
            - secretRef:
                name: netbird-secret
      restartPolicy: OnFailure
