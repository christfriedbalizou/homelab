---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: netbird-apps
spec:
  storageClassName: netbird-apps
  accessModes: ["ReadWriteMany"]
  capacity:
    storage: 1Mi
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: "${NFS_SERVER_IP}"
    path: "/volume1/apps/networking/netbird"
