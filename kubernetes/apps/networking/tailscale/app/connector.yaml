apiVersion: tailscale.com/v1alpha1
kind: Connector
metadata:
  name: connector
spec:
  hostnamePrefix: app-connector
  subnetRoutes:
    advertiseRoutes:
      - 192.168.1.0/24
  exitNode: true
  replicas: 2