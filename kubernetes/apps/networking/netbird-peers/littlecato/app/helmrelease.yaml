---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: littlecato
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: app-template
  values:
    controllers:
      littlecato:
        replicas: 1
        annotations:
          reloader.stakater.com/auto: "true"
        pod:
          affinity:
            podAntiAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
                - labelSelector:
                    matchExpressions:
                      - key: app.kubernetes.io/name
                        operator: In
                        values:
                          - mooncake
                          - littlecato
                          - quinnergon
                  topologyKey: kubernetes.io/hostname
          topologySpreadConstraints:
            - maxSkew: 1
              topologyKey: kubernetes.io/hostname
              whenUnsatisfiable: DoNotSchedule
              labelSelector:
                matchLabels:
                  app.kubernetes.io/name: littlecato
        containers:
          app:
            image:
              repository: netbirdio/netbird
              tag: 0.75.0@sha256:f186c12860d0d0a695a9db29bb32dfe81d8e6fb9d219364020f72c606972acc4
            env:
              NB_MANAGEMENT_URL: https://netbird.${SECRET_DOMAIN}:443
              NB_HOSTNAME: littlecato
              NB_LOG_LEVEL: info
            envFrom:
              - secretRef:
                  name: netbird-peers-secrets
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL
                add:
                  - NET_ADMIN
                  - NET_RAW
                  - SYS_ADMIN
                  - SYS_RESOURCE
            resources:
              requests:
                cpu: 25m
                memory: 105Mi
              limits:
                memory: 500Mi
    persistence:
      tun:
        type: hostPath
        hostPath: /dev/net/tun
        advancedMounts:
          littlecato:
            app:
              - path: /dev/net/tun
      runtime:
        type: emptyDir
        advancedMounts:
          littlecato:
            app:
              - path: /var/log
                subPath: var/log
              - path: /var/run
                subPath: var/run
      config:
        existingClaim: littlecato-jericho
        advancedMounts:
          littlecato:
            app:
              - path: /var/lib/netbird
                subPath: netbird
              - path: /root
                subPath: root
