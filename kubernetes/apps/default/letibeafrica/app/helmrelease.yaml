---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app letibeafrica
spec:
  chartRef:
    kind: OCIRepository
    name: letibeafrica
  interval: 15m
  maxHistory: 2
  timeout: 20m
  install:
    remediation:
      retries: 300
  upgrade:
    remediation:
      retries: 300
  values:
    image:
      repository: bitnamisecure/wordpress
      tag: latest@sha256:d5e0f8bead6110c9034a9dd0f9658dcba98162854f8acc6839c3a703884b9176
    externalDatabase:
      host: mariadb.storage.svc.cluster.local
      database: *app
      user: *app
      existingSecret: &secret letibeafrica-secret
    wordpressUsername: ${LETIBEAFRICA_USER}
    existingSecret: *secret
    wordpressEmail: ${LETIBEAFRICA_EMAIL}
    wordpressFirstName: ${LETIBEAFRICA_FIRST_NAME}
    wordpressLastName: ${LETIBEAFRICA_LAST_NAME}
    wordpressBlogName: ${LETIBEAFRICA_BLOG_NAME}
    allowEmptyPassword: false
    smtpHost: smtp-relay.networking.svc.cluster.local
    smtpPort: "${SMTP_RELAY_PORT}"
    smtpFromEmail: "noreply@${SECRET_DOMAIN}"
    smtpFromName: "noreply"
    ingress:
      enabled: true
      ingressClassName: external
      hostname: "letibeafrica.${SECRET_DOMAIN}"
      annotations:
        external-dns.home.arpa/enabled: "true"
        auth.home.arpa/enabled: "true"
    persistence:
      existingClaim: *app
    mariadb:
      enabled: false