---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app dashboard
  namespace: flux-system
spec:
  components:
    - ../../../../components/ext-auth
    - ../../../../components/protected-gatus
  commonMetadata:
    labels:
      app.kubernetes.io/name: *app
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  path: ./kubernetes/apps/monitoring/dashboard/app
  postBuild:
    substituteFrom:
      - name: cluster-secrets
        kind: Secret
    substitute:
      APP: dashboard
      GATUS_HOST: kubernetes
  targetNamespace: monitoring
  prune: true
  sourceRef:
    kind: GitRepository
    name: home-kubernetes
    namespace: flux-system
  healthChecks:
    - apiVersion: helm.toolkit.fluxcd.io/v2beta2
      kind: HelmRelease
      name: dashboard
      namespace: monitoring
  interval: 30m
  retryInterval: 1m
  timeout: 3m
