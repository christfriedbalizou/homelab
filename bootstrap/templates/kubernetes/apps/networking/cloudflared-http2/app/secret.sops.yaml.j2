---
apiVersion: v1
kind: Secret
metadata:
  name: cluster-cloudflared-http2-tunnel-secret
  namespace: networking
stringData:
  TUNNEL_ID: "{{ bootstrap_cloudflare_http2_tunnel_id }}"
  credentials.json: |
    {
      "AccountTag": "{{ bootstrap_cloudflare_account_tag }}",
      "TunnelSecret": "{{ bootstrap_cloudflare_http2_tunnel_secret }}",
      "TunnelID": "{{ bootstrap_cloudflare_http2_tunnel_id }}"
    }
